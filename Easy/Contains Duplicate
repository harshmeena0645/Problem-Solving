class Solution(object):
    def containsDuplicate(self, nums):
        a=set(nums)
        if len(nums)==len(a):
            return False
        else:
            return True


Time Complexity - O(n)
Space Complexity - O(n)
