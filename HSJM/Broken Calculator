def brokenCalc(start, target):
    steps = 0
    while target > start:
        if target % 2 == 0:
            target //= 2
        else:
            target += 1
        steps += 1
    return steps + (start - target)

start = int(input())
target = int(input())
print(brokenCalc(start, target))
