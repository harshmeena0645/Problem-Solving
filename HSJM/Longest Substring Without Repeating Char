# Unthinkable

def fun(n,str):
    s=''.join(sorted(str))
    sum=0
    for i in range(n):
        sum+=(n-i)*(ord(s[i])-96)
    return sum


if __name__=="__main__":
    n=int(input())
    str=input()
    output=fun(n,str)
    print(output)


Time Complexity - O(n^2)
Space Complexity- O(n)
