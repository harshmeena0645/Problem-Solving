# Unthinkable

def fun(n,k,str):
    s=''.join(sorted(str))
    sum=0
    for i in range(n):
        sum+=(n-i)*(ord(s[i])-96)
    return sum


if __name__=="__main__":
    n=int(input())
    k=int(input())
    str=input()
    output=fun(n,k,str)
    print(output)
